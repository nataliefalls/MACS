#include "BufferedReportQueue.h"

bool BufferedReportQueue::queuePush(const report_t &report) const {
    // stuff
    return false;
}

bool BufferedReportQueue::queuePop(report_t *report) const {
    //stuff
    return false;
}

bool BufferedReportQueue::queuePeek(report_t *report) const {
    //stuff
    return false;
}